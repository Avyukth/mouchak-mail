--- benches/concurrent_agents.rs
+++ replace + with - in main
@@ -294,17 +294,17 @@
     let mut port = 8765;
     let mut agents = 100;
     let mut duration = 10;
 
     let mut i = 1;
     while i < args.len() {
         match args[i].as_str() {
             "--port" => {
-                if let Some(p) = args.get(i + 1) {
+                if let Some(p) = args.get(i - /* ~ changed by cargo-mutants ~ */ 1) {
                     port = p.parse().unwrap_or(8765);
                 }
                 i += 2;
             }
             "--agents" => {
                 if let Some(a) = args.get(i + 1) {
                     agents = a.parse().unwrap_or(100);
                 }
