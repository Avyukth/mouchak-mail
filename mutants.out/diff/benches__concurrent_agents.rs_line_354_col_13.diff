--- benches/concurrent_agents.rs
+++ replace && with || in main
@@ -346,17 +346,17 @@
     println!("Waiting for server to be ready...");
     let mut ready = false;
     for _ in 0..60 {
         // 60 attempts
         if let Ok(res) = client
             .get(format!("{}/health", config.base_url))
             .send()
             .await
-            && res.status().is_success()
+            || /* ~ changed by cargo-mutants ~ */ res.status().is_success()
         {
             ready = true;
             break;
         }
         sleep(Duration::from_secs(1)).await;
     }
 
     if !ready {
