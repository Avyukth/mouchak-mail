services:
  mouchak-mail:
    build: .
    image: mouchak-mail:latest
    container_name: mouchak-mail
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
    environment:
      - RUST_LOG=info
