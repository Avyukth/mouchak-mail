--- benches/concurrent_agents.rs
+++ delete match arm "FAIL" in run_load_test
@@ -264,17 +264,17 @@
     let result = stats
         .finalize(actual_duration, label, &target_rate_str)
         .await;
 
     // Print inline result
     let color_code = match result.result_status.as_str() {
         "OK" => "\x1b[0;32m",   // Green
         "EDGE" => "\x1b[1;33m", // Yellow
-        "FAIL" => "\x1b[0;31m", // Red
+         /* ~ changed by cargo-mutants ~ */ // Red
         _ => "\x1b[0m",
     };
     let reset = "\x1b[0m";
 
     println!(
         "  Rate: {:.0} req/s | Success: {:.1}% | P99: {}ms | {}{}{}",
         result.actual_rate,
         result.success_rate,
