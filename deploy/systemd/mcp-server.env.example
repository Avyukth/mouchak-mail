# Server Configuration
MCP_SERVER_PORT=8000
RUST_LOG=info,mcp_server=debug

# Database
# Ensure the user 'mcp' has read/write access to this file and directory
DATABASE_URL=file:///var/lib/mcp-server/data.db

# Authentication (Optional)
# HTTP_AUTH_MODE=bearer
# HTTP_AUTH_TOKEN=your-secret-token

# Git (Optional, if using git features)
# GIT_AUTHOR_NAME=MCP Agent
# GIT_AUTHOR_EMAIL=agent@localhost
