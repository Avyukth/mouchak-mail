# Server Configuration
MOUCHAK_SERVER_PORT=8765
RUST_LOG=info,mouchak_mail=debug

# Database
# Ensure the user 'mouchak' has read/write access to this file and directory
DATABASE_URL=file:///var/lib/mouchak-mail/data.db

# Authentication (Optional)
# HTTP_AUTH_MODE=bearer
# HTTP_AUTH_TOKEN=your-secret-token

# Git (Optional, if using git features)
# GIT_AUTHOR_NAME=Mouchak Mail
# GIT_AUTHOR_EMAIL=agent@localhost
